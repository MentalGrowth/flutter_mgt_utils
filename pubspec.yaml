name: flutter_mgt_utils
description: "FlutterMgtUtils"
version: 1.0.1

environment:
  flutter: ">=3.24.0"
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: ^2.3.0
